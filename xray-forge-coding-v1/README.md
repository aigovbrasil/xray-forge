# PACKAGE NOTES

This package was normalized from a React interface that embedded multiple system prompts.
The prompts were extracted into explicit internal agent files so the skill can be used as a Claude full-directory package instead of a UI-bound artifact.
